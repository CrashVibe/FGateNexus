name: Release

on:
  push:
    tags:
      - "v*"

permissions:
  contents: write
  packages: write

jobs:
  build-binaries:
    runs-on: ubuntu-latest
    outputs:
      version: ${{ steps.package_version.outputs.version }}
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Bun
        uses: oven-sh/setup-bun@v2
        with:
          bun-version: latest

      - name: Cache bun dependencies
        uses: actions/cache@v4
        with:
          path: ~/.bun/install/cache
          key: ${{ runner.os }}-bun-${{ hashFiles('bun.lockb') }}
          restore-keys: |
            ${{ runner.os }}-bun-

      - name: Install dependencies
        run: bun install --frozen-lockfile

      - name: Get version from tag
        id: package_version
        run: echo "version=${GITHUB_REF_NAME#v}" >> $GITHUB_OUTPUT

      - name: Build for All Platforms
        run: bun run build:all
        env:
          SENTRY_DSN: ${{ secrets.SENTRY_DSN }}
          SENTRY_URL: ${{ secrets.SENTRY_URL }}
          SENTRY_AUTH_TOKEN: ${{ secrets.SENTRY_AUTH_TOKEN }}
          SENTRY_RELEASE: "true"

      - name: Archive and upload artifacts
        run: |
          VERSION=${{ steps.package_version.outputs.version }}
          chmod +r dist/*
          cd dist
          tar -czf ../FGateNexus-v${VERSION}-linux-x64.tar.gz FGateNexus-linux-x64
          tar -czf ../FGateNexus-v${VERSION}-linux-arm64.tar.gz FGateNexus-linux-arm64
          tar -czf ../FGateNexus-v${VERSION}-mac-x64.tar.gz FGateNexus-mac-x64
          tar -czf ../FGateNexus-v${VERSION}-mac-arm64.tar.gz FGateNexus-mac-arm64
          zip -r ../FGateNexus-v${VERSION}-win.zip FGateNexus-win-x64.exe

      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: binaries
          path: |
            FGateNexus-v${{ steps.package_version.outputs.version }}-linux-x64.tar.gz
            FGateNexus-v${{ steps.package_version.outputs.version }}-linux-arm64.tar.gz
            FGateNexus-v${{ steps.package_version.outputs.version }}-mac-x64.tar.gz
            FGateNexus-v${{ steps.package_version.outputs.version }}-mac-arm64.tar.gz
            FGateNexus-v${{ steps.package_version.outputs.version }}-win.zip

  build-docker:
    runs-on: ubuntu-latest
    needs: [build-binaries]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: binaries

      - name: Extract linux-x64 binary for Docker
        run: |
          mkdir -p dist
          tar -xzf FGateNexus-v${{ needs.build-binaries.outputs.version }}-linux-x64.tar.gz -C dist/

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Log in to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Docker metadata
        id: docker_meta
        uses: docker/metadata-action@v5
        with:
          images: wlingxd/fgatenexus
          tags: |
            type=semver,pattern={{version}}
            type=semver,pattern={{major}}.{{minor}}
            type=raw,value=latest

      - name: Build and push Docker image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: ./Dockerfile
          push: true
          tags: ${{ steps.docker_meta.outputs.tags }}
          labels: ${{ steps.docker_meta.outputs.labels }}
          build-args: |
            SENTRY_DSN=${{ secrets.SENTRY_DSN }}
            SENTRY_URL=${{ secrets.SENTRY_URL }}
            SENTRY_AUTH_TOKEN=${{ secrets.SENTRY_AUTH_TOKEN }}
            SENTRY_RELEASE=true
          cache-from: |
            type=registry,ref=wlingxd/fgatenexus:buildcache-amd64
            type=registry,ref=wlingxd/fgatenexus:buildcache-arm64
          cache-to: type=registry,ref=wlingxd/fgatenexus:buildcache-${{ runner.arch }},mode=max

  release:
    runs-on: ubuntu-latest
    needs: [build-binaries, build-docker]
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Download artifacts
        uses: actions/download-artifact@v4
        with:
          name: binaries

      - name: Determine changelog range
        id: changelog_args
        run: |
          TAG_NAME="${GITHUB_REF_NAME}"
          if [[ "${TAG_NAME}" =~ -(alpha|beta|rc) ]]; then
            echo "args=--latest --strip header --tag-pattern ^v[0-9].*" >> "${GITHUB_OUTPUT}"
            echo "prerelease=true" >> "${GITHUB_OUTPUT}"
          else
            echo "args=--latest --strip header --tag-pattern ^v[0-9]+\\.[0-9]+\\.[0-9]+$" >> "${GITHUB_OUTPUT}"
            echo "prerelease=false" >> "${GITHUB_OUTPUT}"
          fi

      - name: Generate Changelog
        uses: orhun/git-cliff-action@v4
        id: git_cliff
        with:
          config: .github/cliff.toml
          args: ${{ steps.changelog_args.outputs.args }}
        env:
          OUTPUT: CHANGELOG.md

      - name: Release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: v${{ needs.build-binaries.outputs.version }}
          name: v${{ needs.build-binaries.outputs.version }}
          draft: true
          generate_release_notes: false
          prerelease: ${{ steps.changelog_args.outputs.prerelease }}
          files: |
            FGateNexus-v${{ needs.build-binaries.outputs.version }}-linux-x64.tar.gz
            FGateNexus-v${{ needs.build-binaries.outputs.version }}-linux-arm64.tar.gz
            FGateNexus-v${{ needs.build-binaries.outputs.version }}-mac-x64.tar.gz
            FGateNexus-v${{ needs.build-binaries.outputs.version }}-mac-arm64.tar.gz
            FGateNexus-v${{ needs.build-binaries.outputs.version }}-win.zip
          body: |
            ## 📦 下载说明

            | 平台 | 架构 | 文件名 |
            |------|------|--------|
            | Linux | x64 | `FGateNexus-v${{ needs.build-binaries.outputs.version }}-linux-x64.tar.gz` |
            | Linux | ARM64 | `FGateNexus-v${{ needs.build-binaries.outputs.version }}-linux-arm64.tar.gz` |
            | macOS | x64 | `FGateNexus-v${{ needs.build-binaries.outputs.version }}-mac-x64.tar.gz` |
            | macOS | ARM64 | `FGateNexus-v${{ needs.build-binaries.outputs.version }}-mac-arm64.tar.gz` |
            | Windows | x64 | `FGateNexus-v${{ needs.build-binaries.outputs.version }}-win.zip` |

            ---

            ## 📋 更新内容

            ${{ steps.git_cliff.outputs.content }}